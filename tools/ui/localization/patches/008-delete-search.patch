--- a/b/src/routes/search/+page.svelte	2026-09-12 22:13:27.031778372 +0000
+++ b/src/routes/search/+page.svelte	2026-09-12 22:15:21.083938852 +0000
@@ -50,7 +50,7 @@
 		if (!conversation) return;
 
 		const confirmed = window.confirm(
-			`Delete "${conversation.name}"? This action cannot be undone.`
+			`Удалить "${conversation.name}"? Это действие нельзя отменить.`
 		);
 
 		if (!confirmed) return;
