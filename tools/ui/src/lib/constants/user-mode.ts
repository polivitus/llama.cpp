/**
 * USER_MODE — переключатель user-friendly интерфейса.
 * 
 * true  — минималистичный UI (для пользователей): без Настроек, Tools, MCP,
 *         System message, выбора модели.
 * false — полный UI (для админа).
 *
 * В ветке ru_userinterface: true.
 * В ветке ru_autolocale:    false.
 */
export const USER_MODE = true;
